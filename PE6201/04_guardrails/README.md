# Part 4 — Guardrails and safety testing

- `cases/` contains the independent guardrail cases.
- `scripts/` contains the deterministic checklist runner.
- `docs/` explains the guardrail design and autonomy gate.
- `results/` contains the V1 and V2 checklists.

Run from the repository root:

```bash
python PE6201/04_guardrails/scripts/run_guardrails.py
```
