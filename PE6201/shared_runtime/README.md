# Shared runtime

These modules are used across Parts 1, 3, 4 and 5. They are stored once here to prevent duplicated, diverging copies.

Part-specific entry scripts add this directory to Python's module search path before importing the shared modules.
